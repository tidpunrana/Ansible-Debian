## tid nginx template
##
## WARNING!!! Do not edit this file directly
## Please COPY to new file and modify whatever you want
##
server {
  listen 80;
  server_name localhost;
  client_max_body_size 300m;
  client_body_buffer_size 128k;
 root /home/Root/domains/website.com/public_html;
  index index.php ndex.html index.htm;
# return 301 https://$server_name$request_uri;


location / {

try_files $uri $uri/ /index.php?$query_string;

  }

 location ~ \.php$ {
    try_files $uri =404;
    fastcgi_pass unix:/var/run/php/7.0-fpm-mvision.sock;
    #fastcgi_pass   127.0.0.1:9000;
    fastcgi_index index.php;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    include fastcgi_params;
}
  location ~ /\.(git|svn|hg|bzr) {
    deny all;
  }


location ~ /.well-known {

                allow all;
}

#  location ~* \.(jpg|png|gif|jpeg|css|js|ico|mp3|wav|swf|mov|doc|pdf|xls|ppt|docx|pptx|xlsx|txt|zip|rar|bz2|gz|dmg|tar|7z|woff|woff2|eot|ttf|cur|svg)(\?.*)?$ {
#    expires 7d;
#  }

#  location ~* \.(html|htm|xml|json)(\?.*)?$ {

#    expires 1h;

#  }
  
  access_log /var/log/nginx/website-access.log ;
  error_log /var/log/nginx/website-error.log ;
}
